/// 复制本文件为 `aliyun_secrets.dart` 并填入你的阿里云 AccessKey。
/// `aliyun_secrets.dart` 已被 .gitignore 忽略，不会提交到 Git。
abstract final class AliyunSecrets {
  static const String accessKeyId = '';
  static const String accessKeySecret = '';
}
